{% liquid
  assign collection = section.settings.collection
  assign products_to_show = section.settings.products_to_show
%}

<section
  class="premium-section premium-featured-products"
  id="PremiumFeaturedProducts-{{ section.id }}"
  style="
    background-color: {{ section.settings.background_color }};
    color: {{ section.settings.text_color }};
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
    --premium-grid-cols: {{ section.settings.columns }};
    --premium-grid-cols-mobile: {{ section.settings.columns_mobile }};
    --premium-card-ratio: {{ section.settings.image_ratio }};
  "
>
  <div class="page-width">
    <header class="premium-featured-products__header">
      <div>
        {% if section.settings.eyebrow != blank %}
          <span class="premium-section__eyebrow">{{ section.settings.eyebrow }}</span>
        {% endif %}
        {% if section.settings.heading != blank %}
          <h2 class="premium-section__heading">{{ section.settings.heading }}</h2>
        {% endif %}
        {% if section.settings.subheading != blank %}
          <p class="premium-section__subheading">{{ section.settings.subheading }}</p>
        {% endif %}
      </div>
      {% if section.settings.button_label != blank %}
        <a href="{{ section.settings.button_link | default: collection.url }}" class="premium-btn premium-btn--secondary premium-featured-products__view-all">
          {{ section.settings.button_label }}
        </a>
      {% endif %}
    </header>

    <div class="premium-featured-products__grid" role="list">
      {% if collection != blank and collection.products_count > 0 %}
        {% for product in collection.products limit: products_to_show %}
          <article class="premium-card premium-featured-products__item" role="listitem">
            <a href="{{ product.url }}" class="premium-featured-products__link">
              <div class="premium-card__image">
                {% if product.compare_at_price > product.price %}
                  <span class="premium-featured-products__badge">Sale</span>
                {% elsif forloop.first %}
                  <span class="premium-featured-products__badge premium-featured-products__badge--soft">Best Seller</span>
                {% endif %}
                {% if product.featured_media %}
                  {{
                    product.featured_media
                    | image_url: width: 800
                    | image_tag:
                      loading: 'lazy',
                      widths: '300, 400, 600, 800',
                      sizes: '(min-width: 750px) 25vw, 50vw',
                      alt: product.featured_media.alt | default: product.title | escape
                  }}
                {% else %}
                  {{ 'product-1' | placeholder_svg_tag }}
                {% endif %}
                <span class="premium-featured-products__quick">Shop now</span>
              </div>
              <div class="premium-featured-products__meta">
                {% if product.vendor != blank %}
                  <p class="premium-featured-products__vendor">{{ product.vendor }}</p>
                {% endif %}
                <h3 class="premium-featured-products__title">{{ product.title }}</h3>
                <p class="premium-featured-products__price">
                  {% if product.compare_at_price > product.price %}
                    <span class="premium-featured-products__price-sale">{{ product.price | money }}</span>
                    <s class="premium-featured-products__price-compare">{{ product.compare_at_price | money }}</s>
                  {% else %}
                    {{ product.price | money }}
                  {% endif %}
                </p>
              </div>
            </a>
          </article>
        {% endfor %}
      {% else %}
        {% for i in (1..4) %}
          <article class="premium-card premium-featured-products__item" role="listitem">
            <div class="premium-card__image">{{ 'product-' | append: i | placeholder_svg_tag }}</div>
            <div class="premium-featured-products__meta">
              <h3 class="premium-featured-products__title">Product {{ i }}</h3>
              <p class="premium-featured-products__price">{{ 1999 | money }}</p>
            </div>
          </article>
        {% endfor %}
      {% endif %}
    </div>
  </div>
</section>

{% stylesheet %}
  .premium-featured-products__header {
    display: flex;
    flex-wrap: wrap;
    align-items: flex-end;
    justify-content: space-between;
    gap: 1.5rem;
    margin-bottom: 2.75rem;
  }

  .premium-featured-products__view-all {
    flex-shrink: 0;
  }

  .premium-featured-products__grid {
    display: grid;
    grid-template-columns: repeat(var(--premium-grid-cols-mobile, 2), 1fr);
    gap: 1rem 0.75rem;
  }

  @media screen and (min-width: 750px) {
    .premium-featured-products__grid {
      grid-template-columns: repeat(var(--premium-grid-cols, 4), 1fr);
      gap: 2rem 1.5rem;
    }
  }

  .premium-featured-products__link {
    display: block;
    text-decoration: none;
    color: inherit;
  }

  .premium-featured-products__item .premium-card__image {
    position: relative;
  }

  .premium-featured-products__badge {
    position: absolute;
    top: 0.75rem;
    left: 0.75rem;
    z-index: 2;
    padding: 0.35rem 0.55rem;
    font-size: 0.625rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    background: #fff;
    color: var(--beauty-ink, #1c1917);
    border-radius: 2px;
  }

  .premium-featured-products__badge--soft {
    background: var(--beauty-bg-blush, #f3ebe6);
    color: var(--beauty-accent, #8f6a5b);
  }

  .premium-featured-products__quick {
    position: absolute;
    left: 0.75rem;
    right: 0.75rem;
    bottom: 0.75rem;
    z-index: 2;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0.7rem 0.75rem;
    background: rgba(255, 255, 255, 0.94);
    color: var(--beauty-ink, #1c1917);
    font-size: 0.6875rem;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    opacity: 0;
    transform: translateY(8px);
    transition: opacity 0.3s ease, transform 0.35s var(--beauty-ease, ease);
  }

  .premium-featured-products__item:hover .premium-featured-products__quick {
    opacity: 1;
    transform: translateY(0);
  }

  .premium-featured-products__meta {
    padding: 1rem 0.15rem 0;
  }

  .premium-featured-products__vendor {
    margin: 0 0 0.25rem;
    font-size: 0.6875rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--beauty-muted, #6b6560);
  }

  .premium-featured-products__title {
    font-size: 0.975rem !important;
    margin: 0 0 0.4rem;
    font-weight: 500;
    letter-spacing: -0.01em;
  }

  .premium-featured-products__price {
    margin: 0;
    font-size: 0.875rem;
    color: var(--beauty-muted, #6b6560);
  }

  .premium-featured-products__price-sale {
    color: var(--beauty-ink, #1c1917);
    margin-right: 0.5rem;
    font-weight: 500;
  }

  .premium-featured-products__price-compare {
    opacity: 0.5;
  }

  @media screen and (max-width: 749px) {
    .premium-featured-products__view-all {
      width: 100%;
      text-align: center;
    }

    .premium-featured-products__quick {
      opacity: 1;
      transform: none;
      position: static;
      margin-top: 0.5rem;
      background: transparent;
      justify-content: flex-start;
      padding: 0;
      font-size: 0.625rem;
    }

    .premium-featured-products__quick::after {
      content: ' →';
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Premium Featured Products",
  "tag": "section",
  "class": "premium-featured-products-section",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Bestsellers"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Featured Products"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "default": "Our most-loved pieces, chosen by you."
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    },
    {
      "type": "range",
      "id": "products_to_show",
      "min": 2,
      "max": 12,
      "step": 1,
      "label": "Products to show",
      "default": 4
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "View all button label",
      "default": "View All"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "View all button link",
      "info": "Defaults to collection URL"
    },
    {
      "type": "range",
      "id": "columns",
      "min": 2,
      "max": 4,
      "step": 1,
      "label": "Columns (desktop)",
      "default": 4
    },
    {
      "type": "select",
      "id": "columns_mobile",
      "label": "Columns (mobile)",
      "options": [
        { "value": "1", "label": "1" },
        { "value": "2", "label": "2" }
      ],
      "default": "2"
    },
    {
      "type": "select",
      "id": "image_ratio",
      "label": "Image ratio",
      "options": [
        { "value": "1/1", "label": "Square" },
        { "value": "4/5", "label": "Portrait" },
        { "value": "3/4", "label": "Tall portrait" }
      ],
      "default": "4/5"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background",
      "default": "#fafafa"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "#1a1a1a"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "Padding top",
      "default": 64
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "label": "Padding bottom",
      "default": 64
    }
  ],
  "presets": [
    {
      "name": "Premium Featured Products"
    }
  ]
}
{% endschema %}
